import styled from "styled-components";

import Banner from "@atlaskit/banner";

export const NotificationsContainer = styled(Banner)`
  position: fixed !important;
  bottom: 0;
  right: 0;
  max-width: 400;
  margin: auto;
`;
